[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --cov=server --cov=ontology --cov=actions --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.report]
exclude_lines = ["pragma: no cover", "if __name__ == .__main__.:"]
